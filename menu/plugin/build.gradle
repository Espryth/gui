plugins {
    id("com.github.johnrengelman.shadow") version("7.0.0")
}

dependencies {
    compileOnly 'org.spigotmc:spigot-api:1.8.8-R0.1-SNAPSHOT'
    implementation project(':menu:api')

    ['1_8_R3'].forEach {
        runtimeOnly project(":menu:runtime:adapt-v$it")
    }
}